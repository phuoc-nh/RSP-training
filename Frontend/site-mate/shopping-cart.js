// “Design a shopping cart checkout system. It should support multiple payment methods (Strategy), send notifications to users and analytics services (Observer), and ensure there’s only one Cart instance per session (Singleton). Apply SOLID so the system is extensible.”

